# Student-Management-REST-API
A FastAPI-based Student Management REST API with CRUD operations, automatic percentage calculation, CGPA generation, grade classification, subject validation, and JSON-based data storage.
